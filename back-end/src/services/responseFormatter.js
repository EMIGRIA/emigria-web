/**
 * ResponseFormatter
 * Role: Merge all pipeline outputs (Gemini extraction, ML fraud score,
 *       geographic risk score, reality check results) into a single
 *       clean unified JSON response for the client
 * Status: Skeleton — implementation pending
 * Dependencies: All upstream services must return their results first
 */
