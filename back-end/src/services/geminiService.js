/**
 * GeminiService
 * Role: Call the Google Gemini 1.5 Flash API with the normalized input payload
 *       and return structured JSON containing extracted job offer fields
 *       (position, salary, country, agency, requirements, etc.)
 * Status: Skeleton — implementation pending
 * Dependencies: GEMINI_API_KEY from env, axios for HTTP requests
 */
